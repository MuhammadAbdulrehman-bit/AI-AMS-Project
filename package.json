{
  "name": "ai-ams-demo",
  "version": "1.0.0",
  "description": "AI-Enabled Appointment Management System — CDNS Demo",
  "scripts": {
    "start": "serve . --listen $PORT"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
