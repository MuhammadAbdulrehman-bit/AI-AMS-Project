{
  "name": "ai-ams-demo",
  "version": "1.0.0",
  "description": "AI-Enabled Appointment Management System — CDNS Demo",
  "scripts": {
    "start": "npx serve . -p ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
